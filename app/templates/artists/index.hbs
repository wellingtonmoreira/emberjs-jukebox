{{buttons/button-success 
    submit=(action 'newArtist')
    label='New'}}

<div class="table-responsive">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>#</th>
                <th>Name</th>
                <th>Image URL</th>
                <th class="text-right">Actions</th>
            </tr>
        </thead>
        <tbody>
            {{#each model as |artist|}}
                <tr>
                    <td>{{artist.id}}</td>
                    <td>{{artist.artistName}}</td>
                    <td>{{artist.imageUrl}}</td>
                    <td class="text-right">
                        <button {{action 'editArtist' artist.id}} type="button" class="btn btn-link btn-custom">
                            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                        </button>
                        <button {{action 'deleteArtist' artist.id}} type="button" class="btn btn-link btn-custom">
                            <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                        </button>
                    </td>
                </tr>
            {{/each}}
        </tbody>
    </table>
</div>